# Local-development deps (NOT installed in the Docker image; the image runs
# pure stdlib + the Ollama binary). Install these only to exercise the T1
# non-ollama path (torch/transformers) or the T2 Fireworks path (openai).
torch>=2.5
transformers>=4.44
peft>=0.12            # only if FINETUNE_ADAPTER_PATH is set
openai>=1.0           # only imported when ENABLE_T2=1
joblib>=1.3           # only imported when LOCAL_T1_MULTISTEP_ENABLED=0